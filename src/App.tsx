import { add, format } from "date-fns";
import BabylonScene from "./BabylonScene";

const App: React.FC = () => {
  return (
    <div className="App">
      <BabylonScene />
    </div>
  );
};

export default App;
